/**
 * External integrations (Shopify, Xero, Klaviyo, Brevo) are wired up in
 * later stages. This module is a placeholder so the folder exists and later
 * stages have an obvious home for each client + IntegrationSyncLog writes.
 */
export {};
